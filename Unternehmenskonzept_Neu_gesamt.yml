extends: default
footer:
  font_size: 9
  recto:
    content:
      left: "{page} von {total-pages}"
      center: "$current-chapter"
      right: "{page} von {total-pages}"
  verso:
    content:
      left: "{page} von {total-pages}"
      center: "$current-chapter"
      right: "{page} von {total-pages}"